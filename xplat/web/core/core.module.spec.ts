describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { ContactCoreModule } from './core.module';

describe('ContactCoreModule', () => {
  it('should work', () => {
    expect(new ContactCoreModule(null)).toBeDefined();
  });
});
*/
